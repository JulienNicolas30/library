{% extends 'base.html.twig' %} {% block title %}Emprunt{% endblock %} {% block
body %}

<h1>Emprunter un livre</h1>

{{ include("borrow/_form.html.twig") }}
{% if is_granted('ROLE_ADMIN') %}
 {# Affiche tous les messages flash de type'error' #} 
 {% for message in app.flashes('error') %}
<div class="alert alert-danger">
  {{ message } <a href="{{ path('app_borrow_index') }}">Retour à la liste</a>}
</div>
{% endfor %}{% endif %} {% endblock %}
